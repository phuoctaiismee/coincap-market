import React from "react";
import Bounded from "../containers/bounded";

const SiteHeader = () => {
  return (
    <div>
      <Bounded>SiteHeader</Bounded>
    </div>
  );
};

export default SiteHeader;
