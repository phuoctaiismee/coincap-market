import React from "react";
import Bounded from "../containers/bounded";

const SiteFooter = () => {
  return (
    <div>
      <Bounded>SiteFooter</Bounded>
    </div>
  );
};

export default SiteFooter;
